/**
 * container の中身を空にしてから、members の一覧を <li> として描画します。
 * 各 <li> のテキストは `${name} (${score}点)` としてください。
 *
 * @param {HTMLElement} container
 * @param {{ name: string, score: number }[]} members
 */
export function renderMemberList(container, members) {
    // TODO: ここを実装してください
}
