# 12. モジュール

対応セクション: `10.javascript.md` の「モジュール」


課題
--------------------------------------------------------------------------------

`formatters.js`には、あらかじめ2つの関数が用意されています（変更不要です）。

- 名前付きエクスポート: `formatCurrency(amount)` → `"¥1,000"`のような文字列を返す
- デフォルトエクスポート: `ratio`（0〜1の割引率）を受け取り`"10%"`のような文字列を返す関数

`todo.js`を実装してください。

1. ファイル冒頭で、`formatters.js`から
   - `formatCurrency`を**名前付きインポート**してください
   - デフォルトエクスポートされた関数を`formatPercentage`という名前で**インポート**してください
2. `describeDiscount(amount, discountRatio)`を実装し、
   上記2つの関数を使って`"¥1,000 (10%引き)"`のような文字列を返してください。


確認方法
--------------------------------------------------------------------------------

```bash
node exercises/12-module/check.js
```
