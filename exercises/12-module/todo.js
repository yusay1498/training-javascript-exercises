// TODO: formatters.js から formatCurrency を名前付きインポートしてください

// TODO: formatters.js のデフォルトエクスポートを formatPercentage という名前でインポートしてください

/**
 * amount（金額）と discountRatio（割引率、0〜1）を受け取り、
 * "¥1,000 (10%引き)" のような文字列を返します。
 *
 * @param {number} amount
 * @param {number} discountRatio
 * @returns {string}
 */
export function describeDiscount(amount, discountRatio) {
    // TODO: ここを実装してください（formatCurrency と formatPercentage を使う）
}
