/**
 * arr1 と arr2 をスプレッド構文で結合し、重複を取り除いた新しい配列を返します。
 * 元の arr1・arr2 は変更してはいけません。
 *
 * @param {unknown[]} arr1
 * @param {unknown[]} arr2
 * @returns {unknown[]}
 */
export function mergeUnique(arr1, arr2) {
    // TODO: ここを実装してください
}

/**
 * user を直接変更せず、changes の内容で上書きした新しいオブジェクトを返します。
 *
 * @param {object} user
 * @param {object} changes
 * @returns {object}
 */
export function updateUser(user, changes) {
    // TODO: ここを実装してください
}

/**
 * 配列の分割代入を使って、先頭の要素と残りの要素を取得します。
 *
 * @param {unknown[]} arr
 * @returns {{ first: unknown, rest: unknown[] }}
 */
export function getFirstAndRest(arr) {
    // TODO: ここを実装してください
}
