// TODO: 関数宣言で add(a, b) を実装してください（a + b を返す）
export function add(a, b) {

}

// TODO: 関数式で multiply を実装してください（a * b を返す）
export const multiply = function (a, b) {

}

// TODO: アロー関数で square を実装してください（x * x を返す）
export const square = (x) => {

}

/**
 * greeting を受け取り、「name を受け取って `${greeting}, ${name}!` を返すアロー関数」を返します。
 *
 * @param {string} greeting
 * @returns {(name: string) => string}
 */
export function makeGreeter(greeting) {
    // TODO: ここを実装してください
}
